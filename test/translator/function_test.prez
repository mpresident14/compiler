void doNothing() {
  return;
}

int inc(int n) {
  return n + 1;
}

bool isEqual(int a, int b) {
  return a == b;
}

void runprez() {
  doNothing();
  printInt(inc(0));

  int a = 4;
  int b = a;
  if (isEqual(a, b)) {
    printInt(2);
  }

  return;
}
