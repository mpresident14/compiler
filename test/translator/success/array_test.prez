int[] createArr() {
  int[] arr = int[5];
  arr[0] = 0;
  arr[1] = 1;
  arr[2] = arr[arr[0]] + arr[arr[1]];
  arr[3] = arr[1] + arr[2];
  arr[4] = arr[2] + arr[3];
  return arr;
}

void printArr(int[] arr) {
  printInt(arr[0]);
  printInt(arr[1]);
  printInt(arr[2]);
  printInt(arr[3]);
  printInt(arr[4]);
}

void runprez() {
  printArr(createArr());

  char[] charArr = char[3];
  charArr[0] = 'a';
  if (charArr[0] == 'a') {
    printInt(5);
  }
}
