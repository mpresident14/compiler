%before {
  #include <cstdlib>
}

%tokens {
  PLUS "+"
  INT  "[1-9][0-9]*" -> int { atoi(str.c_str()); }
}

%grammar{

  Term -> Term
  Term := INT { TInt($0) }


  Expr -> Expr
  Expr := Term { ETerm($0) }
        | Expr PLUS Term { EPlus($0, $2) }

}

